#include<bits/std++.h>
using namespace std;
void phone(int digit,int output,int nums,int ans,int index){
    if(index>=nums.size()){
        ans.push.back(nums);
        return;
        for(int j=0;j<n.size();j++){
            digit
            solve
        }
    }
}
int main(){
    int ans;
    int index=0;

     solve(digit,output,nums,ans,0);
     return ans;

}